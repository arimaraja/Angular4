import { RanpicDirective } from './ranpic.directive';

describe('RanpicDirective', () => {
  it('should create an instance', () => {
    const directive = new RanpicDirective();
    expect(directive).toBeTruthy();
  });
});
